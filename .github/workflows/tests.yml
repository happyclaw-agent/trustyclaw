name: Tests

on: [push, pull_request]

jobs:
  smoke-test:
    name: smoke-test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Run smoke tests
        run: |
          python scripts/smoke_tests.py

      - name: Run simple unit tests
        run: |
          python src/tests/unit/test_simple.py

      - name: Verify imports
        run: |
          python -c "
          from src.clawtrust.sdk.client import SolanaClient
          from src.clawtrust.sdk.identity import AgentIdentity
          from src.clawtrust.sdk.reputation import ReputationEngine
          from src.clawtrust.sdk.escrow import EscrowClient
          print('All imports successful')
          "
